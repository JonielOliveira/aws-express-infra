# ======================
# Node / frontend
# ======================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/

# ======================
# Terraform
# ======================
# states, locks, diretório local do provider
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
# planos binários
*tfplan
*.tfplan
*.tfplan.*
# variáveis locais/sensíveis
terraform.tfvars
terraform.tfvars.* 
!terraform/terraform.tfvars.example
# backup
terraform.tfstate.backup

# ======================
# Ansible
# ======================
*.retry
*.vault.pass
# inventários contendo IPs/credenciais (se quiser manter fora do repo)
ansible/inventory/*
!ansible/inventory/hosts.example   # mantenha um exemplo sem dados sensíveis
group_vars/*/*.vault.yml
host_vars/*/*.vault.yml

# ======================
# Chaves privadas / certificados / segredos
# ======================
# nunca commitar chaves privadas, arquivos PEM, certificados e stores
*.pem
*.key
*.crt
*.csr
*.pfx
*.p12
*.jks
# variáveis de ambiente
.env
.env.*.local
.env.*.secret

# ======================
# Editor / OS
# ======================
# macOS
.DS_Store
# Windows
Thumbs.db
# VSCode / IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ======================
# Logs / tmp / coverage
# ======================
logs
*.log
*.out
*.err
tmp/
temp/
coverage/

# ======================
# Outros
# ======================
*.bak
*.swp
*.zip
